import styled from "styled-components"
import Skeleton from "../../../../components/Skeleton"

export const CustomSkeleton = styled(Skeleton)`
  border-radius: 1.5rem;
`;

export const CustomBarSkeleton = styled(Skeleton)`
  border-radius: 1rem;
  padding: 0;
`;